# retrieve the base image scratch (intentionally empty image on Docker Hub)
FROM ubuntu

MAINTAINER joseph will <this.isthe.email@gmail.com>

RUN apt-get update		##executed during image building

CMD ["echo", "Hello World.."]	##executed only after a container of the image is created

